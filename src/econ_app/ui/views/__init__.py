"""View widgets for the main content area.

Each view is a QWidget with:
- a `view_name` attribute for identification
- a `sidebar_widget` attribute that MainWindow places into the sidebar when the view is active
"""
